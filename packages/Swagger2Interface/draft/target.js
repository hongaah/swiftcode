const filename = '列表 req'

const filters = [{ label: '', key: '' }]

const properties = {
  outDetailType: {
    type: 'integer',
    format: 'int32',
    description: '类型',
    allowEmptyValue: false,
  },
}
export { filters, properties, filename }
